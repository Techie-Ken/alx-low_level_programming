#include "main.h"

/**
 * sub - subtracts 2 integers
 * @a: first integer
 * @b: second integer
 *
 * Return: difference of 2 integers
 */

int sub(int a, int b)
{
	int diff = a - b;

	return (diff);
}
