#include <stdio.h>

/**
 * main - entry point
 * @argc: number of paramethers
 * @argv: string of parameters
 * Return 0
 */

int main(int argc, char *argv[])
{
	int a = 0;

	while (i < argv)
	{
		printf("%s\n", argv[i]);
		i++;
	}
	return (0);
}
