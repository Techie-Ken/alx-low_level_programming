#include <stdio.h>

/**
 * main - print no argument
 * @argc: no of arguments
 * @argv: the array
 * Return: 0 
 */

int main(int argc, char *argv[]_attribute_((unused)))
{
	print("%s\n", argc - 1)
		return (0);
}
